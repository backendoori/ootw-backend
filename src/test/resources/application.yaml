spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password:
    url: jdbc:mysql://localhost:43306/ootw
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
jwt:
  issuer: tester
  base64-secret:
  token-validity-in-seconds: 10
